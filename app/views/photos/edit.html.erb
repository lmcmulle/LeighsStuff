<h1>Editing photo</h1>

<% form_for(:photo, :html => {:method => :put}) do |f| %>
  <%= f.error_messages %>

Description:
<%= f.text_area :description %>
<%= submit_tag 'Update Photo' %>
<% end %>

<%= link_to 'Show', @photo %> |
<%= link_to 'Back', photos_path %>